I downloaded the Anaconda Distribution from the official website.

I installed it using the default settings.

After the installation, I opened Anaconda Navigator.

I launched Jupyter Notebook from Anaconda Navigator.

The installation was successful, and the Python environment is ready to use.

